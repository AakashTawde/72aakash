# Optional: point the client at your deployed Cloud Function proxy.
# After running `firebase deploy --only functions`, copy the URL here.
# Example:
# VITE_PROXY_URL=https://us-central1-your-project.cloudfunctions.net/liquipediaProxy
VITE_PROXY_URL=
